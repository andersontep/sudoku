machine:
  python:
    version: 3.4.0

test:
  pre:
    - pip install tox
    - export DATABASE_URL=postgres://postgres@localhost/test
    - psql -c 'create database test;' -U postgres
  override:
    - tox
  post:
    - mv coverage.html $CIRCLE_ARTIFACTS

